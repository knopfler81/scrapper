<h1>Concerts</h1>


<ol>
  <% @concerts.each do |concert| %>

    <li><%= concert.date.strftime("%a %d %B %Y") %> | <%= concert.city_name %> | <%= concert.department %> | <%= concert.location_name %></li>

  <% end %>
</ol>

